window.Constants = {
  loadingImagePath: "<%= image_path("loading.gif") %>",
  apiBasePath: '/api',
  staticBasePath: '/static',
  pandaAccessDetails: <%= Panda.signed_params('POST', '/videos.json').to_json.html_safe %>,
  pusher: {
    key: '<%= Pusher.key %>'
  }
};
